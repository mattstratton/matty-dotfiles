# Agent detection - only activate minimal mode for actual agents
if [[ -n "$npm_config_yes" ]] || [[ -n "$CI" ]] || [[ "$-" != *i* ]]; then
  export AGENT_MODE=true
else
  export AGENT_MODE=false
fi

ZSH_THEME=""
ZSH_DISABLE_COMPFIX="true"

fpath=($HOME/.docker/completions $fpath)
export ZSH=$HOME/.oh-my-zsh
DEFAULT_USER="mattstratton"

plugins=(autojump colored-man-pages colorize git git-extras docker docker-compose github golang history history-substring-search nvm vscode zsh-syntax-highlighting zsh-autosuggestions)

zstyle ':omz:plugins:nvm' autoload yes

source $ZSH/oh-my-zsh.sh

export GOROOT="/usr/local/go"
export PATH="$GOROOT/bin:$PATH"
alias python="python3"

source ~/.zshrc_shared
